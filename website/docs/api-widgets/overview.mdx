---
sidebar_position: 0
---

# 導覽

將實用的元件基於 Flutter 的 `StatefulWidget` 與 `StatelessWidget` 包裝。v3 全面改用 Material Design 3 的 `Theme.of(context).colorScheme` 取得主題色彩。

:::caution 部分元件使用時必須先完成 
 - [主題色設定](../installation/theme)
 - [多國語言設定](../installation/localization)
:::
